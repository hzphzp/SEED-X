{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: InstructPix2Pix Inference",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/inference/eval_seed_x_detokenizer.py",
            "args": [], // 在这里添加任何需要的命令行参数
            "console": "integratedTerminal"
        }
    ]
}